---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: elements
spec:
  rules:
    - host: chat.haardiek.org
      http:
        paths:
        - pathType: Prefix
          path: /
          backend:
            service:
              name: elements
              port:
                number: 80
  tls:
    - hosts:
        - chat.haardiek.org
      secretName: elements-certificate
