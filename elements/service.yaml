---
apiVersion: v1
kind: Service
metadata:
  name: elements
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
      appProtocol: http
  selector:
    app.kubernetes.io/name: elements
    app.kubernetes.io/instance: matrix
