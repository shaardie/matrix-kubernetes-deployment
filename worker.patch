--- config/homeserver.yaml	2021-10-24 10:41:35.773885120 +0200
+++ worker/homeserver.yaml	2021-10-24 10:44:14.050071223 +0200
@@ -46,7 +46,7 @@
 
 # When running as a daemon, the file to store the pid in
 #
-pid_file: /data/homeserver.pid
+pid_file: /tmp/homeserver.pid
 
 # The absolute URL to the web client which /_matrix/client will redirect
 # to if 'webclient' is configured under the 'listeners' configuration.
@@ -56,7 +56,7 @@
 # under the 'listeners' configuration, however this is a security risk:
 # https://github.com/matrix-org/synapse#security-note
 #
-#web_client_location: https://riot.example.com/
+web_client_location: https://chat.example.de/
 
 # The public-facing base URL that clients use to access this Homeserver (not
 # including _matrix/...). This is the same URL a user might enter into the
@@ -65,7 +65,7 @@
 # Otherwise, it should be the URL to reach Synapse's client HTTP listener (see
 # 'listeners' below).
 #
-#public_baseurl: https://example.com/
+public_baseurl: https://matrix.example.de/
 
 # Set the soft limit on the number of file descriptors synapse can use
 # Zero is used to indicate synapse should set the soft limit to the
@@ -294,6 +294,12 @@
       - names: [client, federation]
         compress: false
 
+  - port: 9093
+    type: http
+    resources:
+      - names: [replication]
+
+
     # example additional_resources:
     #
     #additional_resources:
@@ -742,25 +748,25 @@
 #
 # Example Postgres configuration:
 #
-#database:
-#  name: psycopg2
-#  txn_limit: 10000
-#  args:
-#    user: synapse_user
-#    password: secretpassword
-#    database: synapse
-#    host: localhost
-#    port: 5432
-#    cp_min: 5
-#    cp_max: 10
+database:
+  name: psycopg2
+  txn_limit: 10000
+  args:
+    user: synapse_user
+    password: synapse
+    database: synapse
+    host: postgresql
+    port: 5432
+    cp_min: 5
+    cp_max: 10
 #
 # For more information on using Synapse with Postgres,
 # see https://matrix-org.github.io/synapse/latest/postgres.html.
 #
-database:
-  name: sqlite3
-  args:
-    database: /data/homeserver.db
+#database:
+#  name: sqlite3
+#  args:
+#    database: /data/homeserver.db
 
 
 ## Logging ##
@@ -2559,6 +2565,19 @@
 
 ## Workers ##
 
+worker_app: synapse.app.generic_worker
+worker_name: worker
+
+# The replication listener on the main synapse process.
+worker_replication_host: synapse-main
+worker_replication_http_port: 9093
+
+worker_listeners:
+  - type: http
+    port: 8083
+    resources:
+      - names: [client, federation]
+
 # Disables sending of outbound federation transactions on the main process.
 # Uncomment if using a federation sender worker.
 #
@@ -2610,17 +2629,17 @@
 redis:
   # Uncomment the below to enable Redis support.
   #
-  #enabled: true
+  enabled: true
 
   # Optional host and port to use to connect to redis. Defaults to
   # localhost and 6379
   #
-  #host: localhost
-  #port: 6379
+  host: redis
+  port: 6379
 
   # Optional password if configured on the Redis instance
   #
   #password: <secret_password>
 
 
-# vim:ft=yaml
\ No newline at end of file
+# vim:ft=yaml
